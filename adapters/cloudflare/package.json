{
  "name": "@uptimebuddy/cloudflare-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "@uptimebuddy/checker-core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.90.0",
    "typescript": "^5.5.0"
  }
}
